/**
 * Un fichier importé en TEXTE (`?raw`, résolu par Vite sous Vitest) : la
 * feuille de styles que `contrast.test.ts` mesure telle qu'elle est écrite.
 */
declare module '*.css?raw' {
  const content: string;
  export default content;
}
